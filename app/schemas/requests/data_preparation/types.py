"""
Data preparation shared types.

This module defines shared type aliases used across data preparation schemas.
"""

import datetime

type ValidDataTypes = int | float | str | bool | datetime.datetime
